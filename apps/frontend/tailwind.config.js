const config = {
  darkMode: [],
  content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        foreground: "var(--color-foreground)",
      },
    },
  },
  plugins: [],
};

export default config;
